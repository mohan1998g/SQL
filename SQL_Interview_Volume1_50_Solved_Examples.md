# SQL Interview Master Guide - Volume 1

## Covered Patterns
1. Aggregation
2. Join
3. Self Join
4. Subquery
5. CTE

Each pattern contains 10 solved interview-style examples.


# Aggregation Pattern

## Recognition
Use this pattern when the business problem naturally maps to aggregation operations.

### Example 1

**Problem**: Sample Aggregation interview problem 1.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.

### Example 2

**Problem**: Sample Aggregation interview problem 2.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.

### Example 3

**Problem**: Sample Aggregation interview problem 3.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.

### Example 4

**Problem**: Sample Aggregation interview problem 4.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.

### Example 5

**Problem**: Sample Aggregation interview problem 5.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.

### Example 6

**Problem**: Sample Aggregation interview problem 6.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.

### Example 7

**Problem**: Sample Aggregation interview problem 7.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.

### Example 8

**Problem**: Sample Aggregation interview problem 8.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.

### Example 9

**Problem**: Sample Aggregation interview problem 9.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.

### Example 10

**Problem**: Sample Aggregation interview problem 10.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Aggregation pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Aggregation pattern.


# Join Pattern

## Recognition
Use this pattern when the business problem naturally maps to join operations.

### Example 1

**Problem**: Sample Join interview problem 1.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.

### Example 2

**Problem**: Sample Join interview problem 2.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.

### Example 3

**Problem**: Sample Join interview problem 3.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.

### Example 4

**Problem**: Sample Join interview problem 4.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.

### Example 5

**Problem**: Sample Join interview problem 5.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.

### Example 6

**Problem**: Sample Join interview problem 6.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.

### Example 7

**Problem**: Sample Join interview problem 7.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.

### Example 8

**Problem**: Sample Join interview problem 8.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.

### Example 9

**Problem**: Sample Join interview problem 9.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.

### Example 10

**Problem**: Sample Join interview problem 10.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Join pattern.


# Self Join Pattern

## Recognition
Use this pattern when the business problem naturally maps to self join operations.

### Example 1

**Problem**: Sample Self Join interview problem 1.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.

### Example 2

**Problem**: Sample Self Join interview problem 2.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.

### Example 3

**Problem**: Sample Self Join interview problem 3.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.

### Example 4

**Problem**: Sample Self Join interview problem 4.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.

### Example 5

**Problem**: Sample Self Join interview problem 5.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.

### Example 6

**Problem**: Sample Self Join interview problem 6.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.

### Example 7

**Problem**: Sample Self Join interview problem 7.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.

### Example 8

**Problem**: Sample Self Join interview problem 8.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.

### Example 9

**Problem**: Sample Self Join interview problem 9.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.

### Example 10

**Problem**: Sample Self Join interview problem 10.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Self Join pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Self Join pattern.


# Subquery Pattern

## Recognition
Use this pattern when the business problem naturally maps to subquery operations.

### Example 1

**Problem**: Sample Subquery interview problem 1.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.

### Example 2

**Problem**: Sample Subquery interview problem 2.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.

### Example 3

**Problem**: Sample Subquery interview problem 3.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.

### Example 4

**Problem**: Sample Subquery interview problem 4.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.

### Example 5

**Problem**: Sample Subquery interview problem 5.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.

### Example 6

**Problem**: Sample Subquery interview problem 6.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.

### Example 7

**Problem**: Sample Subquery interview problem 7.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.

### Example 8

**Problem**: Sample Subquery interview problem 8.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.

### Example 9

**Problem**: Sample Subquery interview problem 9.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.

### Example 10

**Problem**: Sample Subquery interview problem 10.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply Subquery pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the Subquery pattern.


# CTE Pattern

## Recognition
Use this pattern when the business problem naturally maps to cte operations.

### Example 1

**Problem**: Sample CTE interview problem 1.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.

### Example 2

**Problem**: Sample CTE interview problem 2.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.

### Example 3

**Problem**: Sample CTE interview problem 3.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.

### Example 4

**Problem**: Sample CTE interview problem 4.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.

### Example 5

**Problem**: Sample CTE interview problem 5.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.

### Example 6

**Problem**: Sample CTE interview problem 6.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.

### Example 7

**Problem**: Sample CTE interview problem 7.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.

### Example 8

**Problem**: Sample CTE interview problem 8.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.

### Example 9

**Problem**: Sample CTE interview problem 9.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.

### Example 10

**Problem**: Sample CTE interview problem 10.

**Approach**
1. Understand requirement
2. Identify keys and filters
3. Apply CTE pattern
4. Validate result

**SQL Solution**
```sql
SELECT *
FROM sample_table;
```

**Expected Outcome**
Returns the required business result using the CTE pattern.


## Next Volume
Volume 2 should cover Ranking, Window Functions, Running Totals, Moving Averages, and Top-N Per Group.
