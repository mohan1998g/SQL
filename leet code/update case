UPDATE salary SET sex =
CASE sex
    WHEN 'm' THEN 'f'
    ELSE 'm'
END;

# Write your MySQL query statement below
UPDATE Salary
SET sex = (CASE
           WHEN sex = 'm'then'f'
           else 'm'
           END);
